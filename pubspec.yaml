name: dermascan
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.6.0
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  cloud_firestore: ^6.1.1
  google_sign_in: ^6.1.0
  url_launcher: ^6.2.5
  mailer: ^6.1.0
  image_picker: ^1.0.7
  firebase_storage: ^13.0.0
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  firebase_ai: ^3.8.0
  camera: ^0.11.4
  path_provider: ^2.1.5
  flutter_animate: ^4.5.2
  flutter_local_notifications: ^20.1.0
  rxdart: ^0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  test: ^1.24.9
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Assets section enabled correctly:
  assets:
    - assets/